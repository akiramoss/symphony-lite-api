spring:
  datasource:
    url: jdbc:mysql://localhost:3306/symphony
    username: root
    password: root

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
