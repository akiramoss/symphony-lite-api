version: '3.8'

services:
  db:
    image: mysql:8.0
    container_name: symphony-mysql
    environment:
      MYSQL_DATABASE: symphony
      MYSQL_ROOT_PASSWORD: root
      MYSQL_USER: symphony_user
      MYSQL_PASSWORD: symphony_pass
    ports:
      - "3306:3306"
